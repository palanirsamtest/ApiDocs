MultiQuestionnaireItemsResponse:
  type: object
  description: The data associated with the resource.
  required:
    - data
  properties:
    data:
      type: array
      description: |
        A list of questionnaire items in the questionnaire. The list is ordered by the positions of the questionnaire items. The types of the questionnaire items in the list could be any of the followings:	
           * Numeric Scale Question
           * Simple Question	
           * Choice Question	
           * Page Break	
           * Section

           Questionnaire items that are not `Page Break` or `Section` will include a `question_id` field with an id of this questionnaire item
      items:
        type: object
