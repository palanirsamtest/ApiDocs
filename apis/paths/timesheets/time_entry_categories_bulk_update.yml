post:
  tags:
    - Timesheets
  operationId: bulkUpdateTimeEntryCategories
  summary: Bulk update/create non-project time categories
  description: |
    Update or create a list of non-project time categories.
  parameters:
    - $ref: '../../components/parameters.yml#/orgIdParam'
  requestBody:
    description: The data required to create or update non-project time category.
    required: true
    content:
      application/vnd.api+json:
        schema:
          $ref: '../../components/schemas.yml#/BulkUpdateTimeEntryCategoriesRequest'
  responses:
    207:
      description: Multi-Status.
      content:
        application/vnd.api+json:
          schema:
            $ref: '../../components/schemas.yml#/BulkUpdateTimeEntryCategoriesResponse'
    401:
      $ref: '../../components/responses.yml#/BadCredentials'
    403:
      $ref: '../../components/responses.yml#/Forbidden'
    404:
      $ref: '../../components/responses.yml#/NotFound'
    415:
      $ref: '../../components/responses.yml#/NotJsonapiMediaType'
